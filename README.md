# Dutchie Interview Project

## Getting started:

Install dependencies and run the React frontend:

```bash
pnpm install && pnpm start
```

In a separate terminal, run the following to start the Node server:

```bash
pnpm run server
```

Visit http://localhost:4200/ and follow the instructions!
